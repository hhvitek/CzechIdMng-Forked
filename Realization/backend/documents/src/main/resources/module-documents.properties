# module properties - not editable through configuration service (idm.pub. prefix is not used)
# mapping pom.xml properties by default
# add custom properties if needed
#
# module version
module.documents.build.version=@project.version@
# build number
module.documents.build.buildNumber=@buildNumber@
module.documents.build.buildTimestamp=@timestamp@
# module vendor
module.documents.build.vendor=@project.organization.name@
module.documents.build.vendorUrl=@project.organization.url@
module.documents.build.vendorEmail=info@bcvsolutions.eu
# module description
module.documents.build.name=@project.name@
module.documents.build.description=@project.description@
#
